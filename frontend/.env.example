# Backend origin only (paths like /api/kotak are added in code). Local default: http://localhost:4000
# VITE_API_BASE_URL=http://localhost:4000

# Kite Connect: backend origin (default: same as VITE_API_BASE_URL origin or http://localhost:4000)
# VITE_KITE_API_BASE_URL=http://localhost:4000
